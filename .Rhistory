#
# This is a Shiny web application. You can run the application by clicking
# the 'Run App' button above.
#
# Find out more about building applications with Shiny here:
#
#    http://shiny.rstudio.com/
#
library(shiny)
# Define UI for application that draws a histogram
ui <- fluidPage(
# Application title
titlePanel("Old Faithful Geyser Data"),
# Sidebar with a slider input for number of bins
sidebarLayout(
sidebarPanel(
sliderInput("bins",
"Number of bins:",
min = 1,
max = 50,
value = 30)
),
# Show a plot of the generated distribution
mainPanel(
plotOutput("distPlot")
)
)
)
# Define server logic required to draw a histogram
server <- function(input, output) {
output$distPlot <- renderPlot({
# generate bins based on input$bins from ui.R
x    <- faithful[, 2]
bins <- seq(min(x), max(x), length.out = input$bins + 1)
# draw the histogram with the specified number of bins
hist(x, breaks = bins, col = 'darkgray', border = 'white')
})
}
# Run the application
shinyApp(ui = ui, server = server)
packrat::init()
# loading data
OA_hh <- data.frame(read.csv(here('topic_model_to_Shiny_app','Shiny','static','Leeds_MSOA_2.csv')))
# only first 10 cols are there are no others
police_dataframe <- data.frame(read.csv('/media/sf_Project_1_-_NLP_Safer_Leeds_Crime_MO/Project/Data/Data_v3_year1.csv'))
View(police_dataframe)
# loading data
PC_to_MSOA <- data.frame(read.csv(here('src','PC_to_LSOA_dec2011.csv')))
library(here)
install.packages(c("here", "leaflet", "shiny"))
library(here)
install.packages("packrat")
packrat::disable()
